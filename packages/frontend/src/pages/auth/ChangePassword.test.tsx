import { describe, it, expect } from 'vitest';

describe('ChangePassword Page', () => {
  it('is removed — password infrastructure replaced by OTP (Epic 14)', () => {
    expect(true).toBe(true);
  });
});
